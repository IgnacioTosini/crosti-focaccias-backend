name: Keep Render Service Alive

# Ejecutar cada 30 minutos para mantener el servicio de Render activo
# Optimizado para reducir consumo de cómputo en Neon (83% menos que cada 5 min)
on:
  schedule:
    - cron: '*/30 * * * *'  # Cada 30 minutos
  workflow_dispatch:  # Permitir ejecución manual

jobs:
  keep-alive:
    runs-on: ubuntu-latest
    
    steps:
      - name: Ping Render Service
        run: |
          echo "🔄 Haciendo ping al servicio de Render..."
          
          # Hacer request al endpoint con timeout extendido y reintentos
          # Render puede tardar hasta 60s en despertar desde hibernación
          response=$(curl -s -w "%{http_code}" \
            -H "User-Agent: GitHub-Actions-KeepAlive/1.0" \
            -H "Accept: application/json" \
            --retry 3 \
            --retry-delay 5 \
            --max-time 90 \
            --connect-timeout 30 \
            https://crosti-focaccias-backend.onrender.com/api/focaccias/featured)
          
          http_code="${response: -3}"
          
          if [ "$http_code" = "200" ]; then
            echo "✅ Ping exitoso (HTTP $http_code)"
            echo "🍕 Servicio de focaccias mantenido activo"
          elif [ "$http_code" = "000" ]; then
            echo "⚠️ Timeout o conexión fallida (puede estar despertando)"
            echo "🔄 Esto es normal en la primera ejecución"
            # No fallar - el siguiente ping funcionará
            exit 0
          else
            echo "⚠️ Respuesta HTTP: $http_code"
            echo "🔍 El servicio respondió pero con código inesperado"
            # No fallar el job por errores ocasionales
            exit 0
          fi
          
      - name: Log Status
        run: |
          echo "📊 Keep-alive ejecutado: $(date)"
          echo "🕐 Próximo ping en 30 minutos"
          echo "⚡ Optimizado: reducción del 83% en consumo vs 5 min"
          echo "🎯 Servicio: crosti-focaccias-backend.onrender.com"